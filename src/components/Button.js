import React, {Component} from 'react';

class Button extends React.Component {

  render() {
    return (
      <div>
         <button>
          Click Me!
         </button>
      </div>
    );
  }
};

export default Button;
